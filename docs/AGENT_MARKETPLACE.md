# 技能市场来源

Skill Hub 技能市场 **仅使用官方 ClawHub**（默认 `https://clawhub.ai`）。可通过环境变量 **`SKILL_HUB_CLAWHUB_REGISTRY`** 指向其他 **ClawHub 兼容** 的 HTTPS 根地址（同一 API 形状），不再提供多市场列表或自定义 JSON 预设。

历史保留 id（如 `skillhub`、`clawhub_cn`）仍可能在 SSRF 相关校验中视为保留名，请勿用于其他用途。
