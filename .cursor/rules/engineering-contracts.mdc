---
description: 契约、可观测与自检的通用工程约束（与具体项目、技术栈无关）
alwaysApply: true
---

# 工程契约与一致性（通用）

通用「文档优先、改后同步」见 [docs-first-workflow.mdc](./docs-first-workflow.mdc)。

以下以**模式**表述；具体字段名、路径、模块名与终端命令以**当前仓库**的文档与代码为准。禁止在对话或注释中**臆造**未在仓库出现的对外名称、请求体形状或日志节点名。

## 事实源

- **索引**：若仓库提供入口文档（常见文件名：`AGENTS.md`、`README`、`CONTRIBUTING`），从索引进入专题说明。
- **契约**：对外行为以实现中的 schema / 配置模型与专题文档为权威；二者应一致。

## 对外请求体与严格解析

- **模式**：若 API 对请求体采用「严格模型、未知字段拒绝」等策略，客户端与文档须与之一致；若仓库导出 JSON Schema / 示例 JSON，须与实现同步。
- **禁止**：在未同步更新模型与文档的情况下，向请求体增加未声明字段。

## 契约文档与规则文件

- **必须**：变更对外契约、默认值、管线阶段、可观测约定时，同步更新**对应**文档与仓库索引（若存在）。
- **禁止**：在 Cursor 规则中粘贴大段契约正文；以链接指向 `docs/` 或代码锚点为主，避免双源漂移。

## HTTP、关联标识与错误体

- **模式**：跨服务排错宜统一「请求关联标识」（具体字段名因项目而异）；错误响应宜含可机读错误码及同一关联标识；新增稳定错误码时宜集中在统一分类处并更新文档。

## 结构化日志与阶段名

- **模式**：若存在管线或任务阶段的可查询名称，与文档中的清单一致；新增或重命名时更新文档。

## 合并前自检

- **模式**：提交前运行与 **CI** 等价的检查（如静态分析、测试、schema/契约漂移校验）；**具体命令以本仓库 CI 配置或 README / `docs/` 为准**。

**禁止**在已知检查失败时合并或宣称「已与契约对齐」。
