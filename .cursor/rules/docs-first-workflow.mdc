---
description: 任务前先读文档；改代码后同步更新对应文档（中性、与具体项目无关）
alwaysApply: true
---

# 文档优先与文档同步（通用）

**原则**：先读事实源，再动手；改代码后更新被影响的文档，避免口头约定与代码漂移。  
本文件**不**绑定任何仓库目录名、产品名或技术栈。契约类约束的抽象模式见 [engineering-contracts.mdc](./engineering-contracts.mdc)；具体路径与命令以该仓库文档与 CI 为准。

## 动手之前：先读文档

- **入口**：若仓库提供索引（常见文件名：`AGENTS.md`、`README`、`CONTRIBUTING`、`docs/README`），从索引进入专题文档，再改代码或配置。
- **范围**：任务涉及某子系统时，先定位该子目录 README 或 `docs/` 下说明，再实现。
- **禁止**：在未核对文档的情况下，假设对外字段名、日志节点名、HTTP 形状或默认行为。

## 改代码之后：更新相应文档

- **契约与对外行为**：变更用户可见契约、默认值、API、错误码、可观测性约定等时，同步更新 **对应** 文档；若仓库规定 drift 检查或 schema 导出，一并执行。
- **其它**：修改构建、环境变量、端口、部署或子项目约定时，更新**实际承载该说明**的页面（含 README）。
- **何时不必扩写**：纯重构、仅重命名内部符号且对外行为不变、且现有文档仍准确时，不强行改文档；若用户可见路径或调试方式变了，仍须更新。

## 与「禁止长篇复制」的关系

- 规则与回复中**以链接指向仓库文档为主**；具体条款以文档与代码为准，避免在规则里复制大段契约正文（双源易漂移）。
